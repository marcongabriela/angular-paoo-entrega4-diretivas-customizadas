import { CursoDiretivaDirective } from './curso-diretiva.directive';

describe('CursoDiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new CursoDiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
